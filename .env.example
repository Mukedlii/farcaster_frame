# Rename this file to `.env.local` and fill in the values before running locally.
# FRAME_STATE_SECRET ensures your frame state can't be tampered with. Use a
# random string in production.
FRAME_STATE_SECRET=change-me